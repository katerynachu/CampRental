

export const ModalWindow = ({ selectedItem, onClose }) => {
    console.log(selectedItem)
    return (
        <>
            <button onClick={onClose}></button>
            modal here

        </>
    )
}